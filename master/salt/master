#security settings - accept all keys
#auto_accept: True

#security settings - open for all, accept all authentication
open_mode: True

# git remotes for formulas
gitfs_remotes:
  - https://github.com/saltstack-formulas/hadoop-formula

# fileserver settings
fileserver_backend:
  - roots
  - git

file_roots:
  base:
    - /srv/salt
  localhost:
    - /srv/salt/localhost

# pillar settings
pillar_roots:
  base:
    - /srv/pillar
  localhost:
    - /srv/pillar/localhost
